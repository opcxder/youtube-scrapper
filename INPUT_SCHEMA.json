{
    "title": "YouTube Scraper",
    "description": "Scrape data from YouTube including trending videos, channels, video metadata, and user profiles",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "URLs to start with. Can be YouTube homepage, channel, video, or user profile URLs",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://www.youtube.com/" }
            ]
        },
        "maxVideos": {
            "title": "Maximum videos to scrape",
            "type": "integer",
            "description": "Maximum number of videos to scrape per page/channel",
            "default": 50,
            "minimum": 1,
            "maximum": 1000
        },
        "maxComments": {
            "title": "Maximum comments per video",
            "type": "integer",
            "description": "Maximum number of comments to scrape per video",
            "default": 100,
            "minimum": 0,
            "maximum": 500
        },
        "sortBy": {
            "title": "Sort order",
            "type": "string",
            "description": "Sort order for videos and comments",
            "enum": ["popular", "recent", "mostViewed"],
            "default": "popular"
        },
        "useProxy": {
            "title": "Use proxy",
            "type": "boolean",
            "description": "Whether to use Apify Proxy",
            "default": true
        }
    },
    "required": ["startUrls"]
} 